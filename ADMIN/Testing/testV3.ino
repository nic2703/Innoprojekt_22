#include <PlotterV3.h>

Plt plot = Plt();

void setup() {
  // put your setup code here, to run once:
  plot.draw_line(10000,10000);
  plot.draw_line(Vec(10000,10000));
}

void loop() {
  // put your main code here, to run repeatedly:
}